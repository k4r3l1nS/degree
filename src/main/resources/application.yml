spring:
  jpa:
    hibernate:
      ddl-auto: none

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres

  mvc:
    hiddenmethod:
      filter:
        enabled: true

  flyway:
    locations: classpath:db/migration
    enabled: true